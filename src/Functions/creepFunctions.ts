// ─────────────────────────────────────────────────────────────────────────────
// creepFunctions.ts  –  重构后的主入口文件
//
// 原61KB文件已经重构为模块：
// - movement.ts  - 移动相关 (CostMatrix, 路径寻找, 移动原语)
// - combat.ts    - 战斗相关 (增强, 疏散, 逃跑, 防御)
// - resource.ts  - 资源相关 (收集, 存储, 转移, 扫描)
// 
// 此文件为向后兼容的入口点，导入所有子模块
// ─────────────────────────────────────────────────────────────────────────────

// 导入所有拆分后的模块
import "./creep/index";

// 这个文件现在作为向后兼容的入口点
// 原有的 import "./Functions/creepFunctions" 语句仍然有效

export {};
